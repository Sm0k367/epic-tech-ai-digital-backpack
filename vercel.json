{
  "buildCommand": "cd apps/host && npm install --legacy-peer-deps && npm run build",
  "installCommand": "echo 'install handled in buildCommand'",
  "outputDirectory": "apps/host/.next",
  "framework": "nextjs"
}
