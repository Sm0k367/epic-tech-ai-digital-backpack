# Contributing to Digital Backpack

1. Fork & clone
2. `pnpm i`
3. Branch: `feat/your-module-name`
4. PR against `main` (CI must pass)

We auto-deploy every merge to the edge—ship fast, break nothing.
