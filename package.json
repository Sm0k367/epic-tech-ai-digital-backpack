{
  "name": "epic-tech-ai-digital-backpack",
  "version": "1.0.0",
  "description": "Self-healing creative ecosystem—zero config, infinite scale",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=apps/host",
    "build": "npm run build --workspace=apps/host",
    "start": "npm run start --workspace=apps/host",
    "lint": "npm run lint --workspace=apps/host"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=9.0.0"
  }
}
